#include <stdlib.h>
#include <iostream>
#include <string>
using namespace std;
namespace ariel
{
    string buildHat(int num);
    string buildHead(int num);
    string buildTorso(int num);
    string buildBase(int num);
    string snowman(int num);
}